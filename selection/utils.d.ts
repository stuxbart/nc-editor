import Point from './point';
export declare const samePoint: (p1: Point, p2: Point) => boolean;
/**
 * Return:
 * 0 if points are equal
 * 1 if first point is greater
 * 2 if second point is greater
 * greater - later in text
 */
export declare const pointCompare: (p1: Point, p2: Point) => number;
